# Local UI geocoding configuration
# Prefer LIC_GEOCODE_ENDPOINT for Cloudflare/production so the Google key stays server-side.
LIC_GOOGLE_MAPS_API_KEY=
LIC_GEOCODE_ENDPOINT=
LIC_API_BASE_URL=
LIC_USE_API_ASSERTIONS=0
